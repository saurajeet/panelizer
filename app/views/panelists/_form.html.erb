<%= form_for(@panelist) do |f| %>
  <% if @panelist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@panelist.errors.count, "error") %> prohibited this panelist from being saved:</h2>

      <ul>
      <% @panelist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :mail %><br>
    <%= f.text_field :mail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
